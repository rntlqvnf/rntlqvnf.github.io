# Disabled: this repository is now a redirect-only stub pointing to
# https://githajae.github.io . The Jekyll archive generation is no longer
# needed here. Kept as manual-only so it never auto-runs (and never fails)
# on pushes.
name: Generate Jekyll Archives (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Nothing to do
        run: echo "This repo only redirects to githajae.github.io; no archives to build."
